services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: lms-backend:latest
    container_name: lms-backend
    restart: unless-stopped
    init: true
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 5000
    ports:
      - "5000:5000"
